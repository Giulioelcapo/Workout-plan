import React from "react";
import { Typography } from "@mui/material";

const Mobility = () => {
  return (
    <div>
      <Typography variant="h4">Mobility Page</Typography>
      <p>Contenuto mobilità</p>
    </div>
  );
};

export default Mobility;